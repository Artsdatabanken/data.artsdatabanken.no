import Header from "../components/header";
import React from "react";

export default () => (
  <div style={{ color: `teal` }}>
    <Header />
    <p>S.....about pageuch wow. Very React.</p>
  </div>
);
